import { createTheme } from '@mui/material/styles';

export const globalTheme = createTheme({
  palette: {
    primary: {
      main: '#6750A4'
    },
    secondary: {
      main: '#E8DEF8',
      contrastText: '#1D192B'
    },

  }
});